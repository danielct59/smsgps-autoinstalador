Versão Apache2
bash
Copiar
Editar
sudo apt update -y && sudo apt install -y git && git clone https://github.com/danielct59/smsgps-autoinstalador.git smsgpsautoinstalador && sudo chmod -R 777 smsgpsautoinstalador && cd smsgpsautoinstalador && sudo mv install_apache /install_apache && cd / && sudo rm -rf smsgpsautoinstalador && sudo chmod +x /install_apache && sudo /install_apache
Versão Nginx
bash
Copiar
Editar
sudo apt update -y && sudo apt install -y git && git clone https://github.com/danielct59/smsgps-autoinstalador.git 
